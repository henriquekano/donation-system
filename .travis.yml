dist: trusty
sudo: required
group: deprecated-2017Q4

services:
  - docker
env:
  global:
    - DOCKER_COMPOSE_VERSION=1.13.0
    - DJANGO=1.11.3
    - DISPLAY=:99.0
    - MOZ_HEADLESS=1
  before_install:
    - sudo rm /usr/local/bin/docker-compose
    - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
    - chmod +x docker-compose
    - sudo mv docker-compose /usr/local/bin

script:
  - docker-compose up -d && docker-compose exec prepare_codeclimate_testcoverage.sh
  - docker-compose exec django python coverage run --source 'POSCaD,dbwrapper' --omit 'venv/*' manage.py test
  - docker-compose exec django coverage xml

after_script:
  - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT
